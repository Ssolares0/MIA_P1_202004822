package Estructuras

func ReportesGraphviz() {
	/*
		//primero abrimos nuestro archivo que contiene el disco
		file,err := os.Open("MIA/P1/A.dsk")
		if err != nil {
			fmt.Println(err)
			return
		}
		defer file.Close()
		disco := NewMBR()
		file.Seek(0,0)
		err = binary.Read(file, binary.BigEndian, &disco)
		if err != nil {
			fmt.Println(err)
			return
		}
		sizeMBR := int(disco.MBR_SIZE)
		free := int(disco.MBR_SIZE)
	*/

}
